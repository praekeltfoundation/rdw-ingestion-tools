name: black-lint

on: [push, pull_request]

jobs:
  black-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: psf/black@stable
      with:
        options: "-l79"